// this variable  serves as a counter for tracking the number of likes.
var likeCount = 0;
// this function will increase the number of likes .
function increaseLikes() {
    // this operation will increase by 1the number of count. 
    likeCount = likeCount + 1;
}
